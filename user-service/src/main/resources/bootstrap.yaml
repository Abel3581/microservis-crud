spring:
  application:
    name: user-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: "http://localhost:8180/realms/tutorial-realm"
